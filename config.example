# Example config file for generating application statistics
# Do not use " and use ${....} format so that file can be included into both shell scripts and Makefile

# Start year
sy=2018
# Start month
sm=01
# Start day
sd=01
# End year
ey=${sy}
# End month
em=${sm}
# End day
ed=31

# Number of days
days=31
# Number of nodes reserved for production queue
production_nodes=60

# Type of report: monthly, weekly, aggregate
type=monthly

# If monthly statistics then set monthly=1
monthly=1

# Organization reports
report_org=1

# Deep learning report
report_dl=1

# Queue first run statistics
qfirstrun=1

# To automatically copy files out to stakeholders set publish=1
publish=0

# To remove dependent jobs from graphs of wait statistics set filter_dependent_jobs=1
filter_dependent_jobs=1

# Do not modify the next 2 lines
suffix=${sy}${sm}${sd}-${ey}${em}${ed}
prefix=${ey}-${em}

# Location of application information
csvalljobs=/app/nsccadmin/log/printjob/alljobs.csv
csvproject=/app/nsccadmin/log/printjob/project.csv
csvprojectinfo=/app/nsccadmin/log/printjob/project-info.csv
amslogdir=/app/nsccadmin/log/ams

# Location of output directory
logdir=/app/nsccadmin/log/usage-statistics
statsuser=fsg3
statsgroup=nscc-proj

# used to calculate unused production queue
hours=`expr $days \* 24`
production_cores=`expr $production_nodes \* 24`
