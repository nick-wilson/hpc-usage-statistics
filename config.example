# Example config file for generating application statistics
# Do not use " and use ${....} format so that file can be included into both shell scripts and Makefile

# Start year
sy=2017
# Start month
sm=06
# Start day
sd=01
# End year
ey=${sy}
# End month
em=${sm}
# End day
ed=30

# If monthly statistics then set monthly=1
monthly=0

# To copy files out to stakeholders set publish=1
publish=0

# Do not modify the next 2 lines
suffix=${sy}${sm}${sd}-${ey}${em}${ed}
prefix=${ey}-${em}

# Location of application information
csvalljobs=/app/nsccadmin/log/printjob/alljobs.csv
csvproject=/app/nsccadmin/log/printjob/project.csv
